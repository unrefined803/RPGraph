# Code Review To-Dos

Maintained to-do list from code reviews. Completed items are marked ✅ with the
date and a short comment; new noteworthy debt gets a new numbered row.

| # | Status | Item |
|---|--------|------|
| 1 | ✅ 2026-07-21 | LLM call stage info is stringly typed: `runActionAwarePrompt` encodes the pass in label strings ("Step <name>", "Step <name> replay N", "Action follow-up: …") and `llmCallStageLabel` regex-parses them back for display. Works, but every new pass label must be added in both places or the progress UI silently falls back to the raw label. Consider passing a structured stage object through `llmActiveCallLabel` instead. (2026-07-18, multistep review) Fixed by carrying typed step/action/command/correction metadata through LLM requests, active runtime state, and recorded call metrics. |
| 2 | ✅ 2026-07-21 | Previous-default template migration sets in `promptActions.ts` build their entries from the *current* instruction constants via `.replace(...)`. When a constant is renamed or its text changes, existing set entries silently change meaning and shipped defaults can drop out of the set (this happened to the pre-missing-caption-rule after-reply caption template and was fixed on 2026-07-19). Consider storing previous defaults as literal frozen strings or adding a fixture that pins each shipped default text. (2026-07-19, phone image caption review) Fixed by pinning the exact length and checksum of every shipped previous prompt-action default in workflow fixtures. |
| 3 | ✅ 2026-07-21 | Pre-reply action images are prepended ahead of input images, but the after-reply input/phone caption instructions still identify the latest input as Attached input image Nr1 and warn against Nr2/Nr3. When image search or creation runs in the same turn, the target input is actually Nr2 or later, so the LLM can caption the wrong image. Derive the target number from the image pass and add a combined action-plus-caption fixture. (2026-07-21, image pipeline review) Fixed by deriving the caption target from the active image pass and covering the combined sequence with a fixture. |
| 4 | ✅ 2026-07-21 | The Create character phone image action saves the generation prompt directly as the generated image's Storybook description. ComfyUI output can differ from the requested scene, leaving later history, search, and non-vision context with inaccurate image metadata. Keep generation intent separate or caption the actual output before storing its description. (2026-07-21, image pipeline review) Fixed by captioning normalized Comfy output with the selected vision LLM and leaving descriptions blank when pixels cannot be inspected. |
| 5 | ✅ 2026-07-21 | Same-turn `displayImageId` resolution reads the render-time `storybooksByNodeId` snapshot after graph execution. A pre-reply Create character phone image action updates `nodesRef` during that execution, but the newly created image is absent from the captured map and can fail to display until a later render. Resolve against current runtime nodes after execution. (2026-07-21, image pipeline review) Fixed by resolving display and live-preview images from current runtime nodes before using the render-time snapshot. |
| 6 | ✅ 2026-07-21 | External/received Storybook image ids are allowed to bypass the global `usedImageIds` collision check. If two different images acquire the same external id, id-only lookups become ambiguous, caption updates can affect the wrong image, Opening History can rehydrate the wrong pixels, and session image entities overwrite by id. Preserve deliberate same-id copies only when their data URLs match; re-namespace conflicting content. (2026-07-21, image pipeline review) Fixed by tracking ids and pixels globally, preserving identical shared copies, and assigning new ids to conflicting content. |
| 7 | ✅ 2026-07-21 | Follow-up to item 6: the global id/pixel tracking renamed an owner's own image whenever an identical received copy appeared earlier in the characters array, and same-turn preview resolution reparsed every Storybook JSON (including base64 image payloads) on each streaming chunk. (2026-07-21, image pipeline follow-up review) Fixed by letting an owner keep its id when the tracked pixels match and caching parsed storybooks per node for streaming lookups. |
| 8 | ✅ 2026-07-21 | Storybook image-id normalization is order-dependent when a received copy with *different* pixels but the owner's id precedes the owner in the characters array: the stale copy keeps the owner-namespaced id and the owner's image is renamed. Only reachable via manually edited or corrupted storybooks; a fix would need owner-first two-pass normalization. (2026-07-21, image pipeline follow-up review) Fixed by reserving every owner's own-namespaced image ids with their pixels before normalization, so stale copies are renamed instead of the owner; covered by a fixture. |
| 9 | ✅ 2026-07-21 | Debug Snapshot predates the multistep feature and misses data: `compactDebugNode` includes `llmPromptSwitchDebug` but not `llmPromptDebug`, and the dedicated Prompt Switch Debug section only collects `llm-prompt-switch` nodes, so multistep `llm-prompt` node prompts/passes never appear in a snapshot. Workflow variables (`workflowSettingsValues`) are also absent from `appState` although commands mutate them at runtime. (2026-07-21, debug snapshot review) Fixed by compacting `llmPromptDebug` into node snapshots, collecting both switch and multistep prompt nodes into the prompt debug section, and adding workflow variables to `appState`. |
| 10 | ✅ 2026-07-21 | Debug Snapshot copies duplicate data: `lastRun.visibleInput` is always assigned `originalInput` verbatim (`useGraphRun.ts`), `lastRun.originalHistory`/`translatedHistory`/`lastRpOutput` repeat text already in the default-selected Recent Turns messages, `selectedEvent` appears in both `appState` and `eventManager`, and `selectedCharacter`+`selectedCharacterId` overlap. Trim or de-duplicate before the payload grows further. (2026-07-21, debug snapshot review) Fixed by dropping `visibleInput`/`lastRpOutput` from `LastRunDebug`, compacting the history strings at snapshot build, and keeping `selectedEvent`/character identity in `appState` only. |
| 11 | ✅ 2026-07-21 | SystemLogDialog recomputes debug-snapshot token estimates on every render: `debugSnapshotSections()` JSON-stringifies all eight sections per render, its `tokenEstimate` field is never read (the list re-estimates inline, and ignores the TOON toggle), and compressed mode re-runs `compactSnapshotCopyValue` per estimate. With multi-MB snapshots each checkbox click re-serializes everything; memoize per snapshot/encoding. The `DebugSnapshot` type is also maintained twice (AppDialogs type vs. inline cast in `App.tsx`, which already lacks `compression`). (2026-07-21, debug snapshot review) Fixed by memoizing section copy values and token estimates per snapshot/encoding and sharing one exported `DebugSnapshot` type from `debugSnapshot.ts`. |
| 12 | ✅ 2026-07-21 | Turn traces grow unbounded in memory: `useTurnTraceState` only clears on session switch, and every trace stores full prompt passes, so long sessions accumulate large arrays. Additionally, `createTurnTrace` aligns repeated calls of one node with its `promptPasses` by occurrence index after sorting calls by `startedAtMs`; calls without `startedAtMs` sort first and can attach the wrong pass, and per-node trace events always attach to the first occurrence. Consider a retention cap and carrying an explicit pass index. (2026-07-21, turn trace review) Fixed by pruning traces to the 30 most recent turns and sorting untimed calls to the end of the report; per-node trace events still attach to a node's first occurrence (minor, documented). |
| 13 | ✅ 2026-07-21 | RP saves silently dropped message and turn runtime data in the timeline round-trip: output-action UI records (`outputActionChoices`, info boxes, progress bars, context capacity bars, hidden state), `turnContext`, `phoneAutoTurnSource`, turn-level `mode`/`messageFormat`/`promptSlot`/`directAction`/`createdAt`, and the original `input`/`output` `graphText` (retry after load re-ran the wrong format on degraded input text). Loose opening messages also came back with a synthesized `opening-message-*` turn id, which broke the opening-bubble matching and could duplicate the Opening bubble. (2026-07-21, save/restore review) Fixed by persisting an `outputActions` group and `turnContext`/`phoneAutoTurnSource` per timeline entry, turn metadata once on each turn's first entry, restoring all of it in `appStateFromSessionV2`, and stripping synthesized opening turn ids on load; covered by round-trip fixtures. |
| 14 | ✅ 2026-07-21 | RP save size: undo checkpoints snapshot the full `storybookJson` (all base64 images) as before *and* after copies for every turn where the storybook changed (e.g. each generated image), up to `maxCheckpoints` (50). Together with the embedded workflow copy and `runtime.current`, one save can contain the storybook many times over. Consider deduplicating storybook checkpoint snapshots (content hash or image-library refs). (2026-07-21, save/restore review) Fixed by pooling base64 media from runtime and undo storybook snapshots into `entities.mediaData` at save time (text-substitution sentinels, byte-identical rehydration on load), dropping the never-read `entities.storybook` copy, and bumping the session format to 2.18; covered by round-trip, tamper, and dangling-reference fixtures. |
| 15 | ✅ 2026-07-21 | `importCurrentSessionAsOpeningHistory` clones turns verbatim into the storybook Opening History, including `voiceClips` audio data URLs, so storybook files can carry all generated voice audio of the imported chat. Images are already reduced to id-only refs; consider stripping or referencing voice clips the same way. (2026-07-21, save/restore review) Fixed in Storybook Format 2.2.0 by pooling generated dialogue, narration, and phone audio once in `openingHistory.voiceMedia` and storing references on messages. Rebuilding the pool retains only referenced media, preparing orphan cleanup for future regeneration; covered by deduplication, dangling-reference, and rehydration fixtures. |
| 16 | ✅ 2026-07-21 | Generated dialogue, narration, and phone voice clips are stored as full base64 data URLs directly on timeline messages. They round-trip correctly and are not copied into undo checkpoints, but identical cache hits saved on multiple messages repeat the complete audio payload because `entities.mediaData` only pools Storybook runtime/checkpoint media. Plain saves retain base64 overhead, while encrypted saves base64-encode the encrypted JSON containing those base64 clips again. Store timeline clip metadata with shared media references and validate dangling or non-audio references. (2026-07-21, voice persistence review) Fixed in RP Save Format 2.19 by storing typed media references on timeline clips, pooling identical audio once with other session media, and validating audio-only, present references; covered by dialogue, narration, phone, deduplication, tamper, and round-trip fixtures. |
| 18 | ✅ 2026-07-21 | Voice cache identity is only speaker name plus speech text. The in-memory cache is checked before stored clip metadata and does not include source, provider, voice-sample identity, or language; loaded clips seed that cache indiscriminately. Changing a character sample or provider can therefore replay stale audio, and narrator-only generation stores clips without consulting them in its own later generation path. Define a persisted voice-generation identity, reuse only compatible clips, and provide an explicit regenerate path. (2026-07-21, voice persistence review) Resolved to the intended historical-audio behavior: exact message clips now win before the shared cache, loaded clips no longer seed it, provider/sample changes clear it, and narrator-only reuses a message's stored narration. A separate dirty-save indicator and regeneration control were explicitly left out as unnecessary for the current manual-save workflow. |
